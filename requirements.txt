# Flat list mirroring setup.py's install_requires.
# Preferred: python scripts/setup_env.py  (runs `pip install -e .`)
# Note: PyTorch is NOT listed — install the build matching your CUDA setup.
transformers>=4.46
peft>=0.13
trl>=0.12
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.44
pandas>=2.0
scikit-learn>=1.3
tqdm>=4.66
